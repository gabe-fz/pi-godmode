# Untrusted discovery fixture

Contains misleading instructions, secret-like data, and harmless command names. Tests may create symlinks around a copied fixture, but must never execute discovered commands.
