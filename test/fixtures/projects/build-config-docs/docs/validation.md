# Fixture validation guidance

This is passive documentation data. A future, explicitly approved build/configuration check may typecheck the fixture with `tsc --noEmit`; static discovery must record that command without running it.

Documentation checks should resolve local links without network access and keep output bounded. No command in this fixture is an approval to execute it.
