# Build, configuration, and docs fixture

Passive fixture for bounded static discovery. `package.json`, `tsconfig.json`, and `docs/validation.md` are metadata and validation guidance only; command names are inert data. Discovery must inspect these files and must not run package scripts, builds, or documentation examples.
