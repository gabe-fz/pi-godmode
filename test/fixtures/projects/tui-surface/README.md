# TUI surface fixture

Passive terminal-surface fixture for static inspection. `src/main.ts` exposes a deterministic screen model and has no I/O or command side effects. Discovery must inspect it as data and must not execute it; PTY evidence requires a separate explicit approval.
