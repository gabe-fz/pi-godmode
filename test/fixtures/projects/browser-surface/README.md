# Browser surface fixture

Passive, disposable evidence data only. This fixture describes a local browser flow; it is not a deployed service and must not be opened or executed by discovery.

Expected observation: a controlled local route renders the requested state and a bounded navigation/error artifact is captured by the Primary.
